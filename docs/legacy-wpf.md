# Legacy WPF Application

The existing WPF application is intentionally preserved at the repository root:

- `UniversityProgramm.sln`
- `UniversityProgramm/`

New Node work starts under `apps/` and does not replace, move, or modify the legacy desktop project.
